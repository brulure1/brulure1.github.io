---
import { getCollection } from 'astro:content';

const fiches = (await getCollection('docs')).filter(
	(d) => d.data.answerShort && d.data.category && d.data.category !== 'guide' && !d.data.draft,
);
const count = fiches.length;
const categories = new Set(fiches.map((f) => f.data.category)).size;
---

<div class="sp-stats">
	<div class="sp-stat" data-accent="blue">
		<span class="sp-stat__icon" aria-hidden="true">📊</span>
		<strong>{count}</strong>
		<span>fiches publiées</span>
	</div>
	<div class="sp-stat" data-accent="violet">
		<span class="sp-stat__icon" aria-hidden="true">🗂️</span>
		<strong>{categories}</strong>
		<span>thèmes actifs</span>
	</div>
	<div class="sp-stat" data-accent="emerald">
		<span class="sp-stat__icon" aria-hidden="true">✓</span>
		<strong>100 %</strong>
		<span>sourcées</span>
	</div>
</div>
