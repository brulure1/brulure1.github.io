---
interface Props {
	href: string;
	title: string;
	confidenceClass?: string;
	confidenceLabel?: string;
	accent?: string;
	index?: number;
}

const {
	href,
	title,
	confidenceClass = '',
	confidenceLabel,
	accent = 'default',
	index = 0,
} = Astro.props;

const delay = `${index * 0.05}s`;
---

<li
	class="sp-fiche-card-wrap sp-animate-in"
	data-accent={accent}
	style={`animation-delay: ${delay}`}
>
	<a href={href} class="sp-fiche-card">
		<h3>{title}</h3>
		{confidenceLabel && (
			<span class={`sp-fiche-card__confidence ${confidenceClass}`}>{confidenceLabel}</span>
		)}
	</a>
</li>
